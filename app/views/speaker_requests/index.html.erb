<div class="row-fluid">
<h3>Speaker Requests</h3>
<% if @use_cache == true %>
	<% #cache({:action => 'index'}, :expires_in => 24.hours) do %>
	<% cache('search_sp') do %>
	
	 <%= render :partial => 'search_area', :collection => @speaker_requests_upcoming %>
	<% end %> 
<% else %>
 <%= render :partial => 'search_area', :collection => @speaker_requests_upcoming %>
<% end %>


</div>
